<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.choi.booking.login.mapper.LoginMapper">
	<select id="findById" parameterType="String" resultType="com.choi.booking.login.DTO.UserVO">
		SELECT * FROM FB_USER 
		WHERE FB_USERID=#{userid}
	</select>
</mapper>